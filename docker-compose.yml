version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:7860"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
    volumes:
      - .:/app
